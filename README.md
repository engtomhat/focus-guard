# PR screenshots

This branch only holds screenshots referenced from pull request comments
(`pr-<number>/before/*.png` = `main` at the time, `pr-<number>/after/*.png` = the PR).
It shares no history with `main` and is not part of the extension; it can be
deleted at any time (older PR comments would then show broken images).
